/*
  AI-Bot.js
  المسؤول عن الشات بوت والردود الذكية
*/

// إعدادات الموديل (لو هتستخدم OpenAI API مستقبلاً)
const API_KEY = ""; // سيبها فاضية لو هتجيبها من السيرفر
const SYSTEM_PROMPT = `
أنت "رفيق"، مساعد إسلامي ذكي في تطبيق "تزكية".
هدف: تشجيع المستخدم على الطاعة بلين ورفق.
المحظورات: لا تفتي في الدين، لا تحلل ولا تحرم. 
لو سُئلت عن فتوى، قل: "لا أعلم، يرجى سؤال أهل الذكر".
استخدم اللهجة المصرية الودودة والمحترمة.
`;

export async function sendMessageToBot(userMessage) {
    // محاكاة للرد (Mock Response) لحد ما نربط API حقيقي
    // عشان منضيعش فلوس الـ API وإحنا بنجرب
    
    return new Promise((resolve) => {
        setTimeout(() => {
            let reply = "";
            
            if (userMessage.includes("صلاة") || userMessage.includes("كسلت")) {
                reply = "ولا يهمك يا بطل، قوم دلوقتي اتوضى وصلي ركعتين لله، الصلاة هي اللي هترتب يومك ❤️";
            } else if (userMessage.includes("حزين") || userMessage.includes("مخنوق")) {
                reply = "«ألا بذكر الله تطمئن القلوب». جرب تمسك السبحة وتستغفر 100 مرة، وهتحس بفرق كبير والله.";
            } else {
                reply = "أنا معاك يا صاحبي، قول لي أقدر أساعدك إزاي في يومك النهاردة؟ 🕌";
            }
            
            resolve(reply);
        }, 1500); // تأخير بسيط عشان يبان طبيعي
    });
}